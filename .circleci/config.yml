version: 2
jobs:
    build:
        docker:
            - image: maven:3-jdk-8
        resource_class: medium
        steps:
            - checkout
            - run:
                name: Maven Build
                command: |
                    MVN_OPTS="-gs .circleci/settings.xml -fae -Dupdatesite.path=${P2_URL} -Dlicense.path=./license_RealtimeBigData_20210315 -Dgeneration.type=local -Dtalend.job.context=Linux"
                    DOC_OPTS="-Ddocker.push.username=wokamoto-git -Ddocker.push.password=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 -Ddocker.push.registry=http://ghcr.io/wokamoto-git"
                    mvn ${MVN_OPTS} -f WOKAMOTO_PROJECT/poms/pom.xml deploy -Pdocker -e 
workflows:
    version: 2
    build:
        jobs:
            - build